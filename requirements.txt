# Core CV + numerics
mediapipe>=0.10.18
opencv-python>=4.9
numpy>=1.26

# Model
torch>=2.2

# Training utilities
scikit-learn>=1.4
tqdm>=4.66

# Phase 2: gloss -> English (optional at runtime; falls back if absent/no key)
anthropic>=0.69

# Data acquisition (scripts/download_wlasl.py)
yt-dlp>=2025.1

# Local web UI (asl/web.py)
fastapi>=0.115
uvicorn>=0.30

# Tests
pytest>=8.0
